# ── Core Dependencies ──
pandas>=2.0.0
numpy>=1.24.0
# (plotly da go — backend/frontend khong con phu thuoc; gap thi cai lai)

# ── Statistics ──
scipy>=1.11.0
statsmodels>=0.14.0

# ── Machine Learning ──
scikit-learn>=1.3.0

# ── Database ──
SQLAlchemy>=2.0.0
bcrypt>=4.0.0
cryptography>=42.0.0  # bat buoc: Fernet ma hoa BYOK at-rest (khong fallback plaintext)

# ── API ──
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0
PyJWT>=2.8.0
python-multipart>=0.0.6

# ── AI / LLM (optional) ──
openai>=1.0.0
langchain>=1.0.0
langchain-openai>=0.1.0
langchain-google-genai>=1.0.0

# ── Performance ──
redis>=5.0.0
psutil>=5.9.0

# ── Warehouse (P1) ──
duckdb>=0.10.0
pyyaml>=6.0

# ── Utilities ──
python-dotenv>=1.0.0