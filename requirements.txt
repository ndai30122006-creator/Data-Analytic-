# ── Learning Analytics Thống kê — Dependencies ──
# Requirements are now split across files in requirements/:
#
#   requirements/base.txt    — core runtime dependencies
#   requirements/dev.txt     — development & testing tools
#   requirements/prod.txt    — production-specific packages
#
# Quick install (all):  pip install -r requirements/dev.txt -r requirements/base.txt
# Production install:   pip install -r requirements/prod.txt
# Docker build:         pip install -r requirements/base.txt

-r requirements/base.txt